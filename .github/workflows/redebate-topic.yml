name: redebate-topic
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
on:
  workflow_dispatch:
    inputs:
      topic:
        required: false
        type: string
  schedule:
    - cron: "23 16 * * 2"
permissions:
  contents: read
jobs:
  redebate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Redebate trigger check."
