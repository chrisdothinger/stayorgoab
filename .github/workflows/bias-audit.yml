name: bias-audit
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  bias-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Bias audit stub. Public summaries only."
