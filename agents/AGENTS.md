# StayOrGoAB Public Agent Team

Agents are public workflow identities. They do not receive human approval gates. They act within path scopes, run automated checks, and publish summarized public run records.

Hermes agents should treat this repository as canonical. They may propose or commit changes to YAML, MDX, manifests, and workflow-dispatch inputs according to `registry.yml` and `permissions.yml`.
